import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <main className="bg-[#f5f2ef] w-full min-h-screen">
      <Head>
        <title>Workwolf</title>
      </Head>
      Hello World
    </main>
  );
};

export default Home;
